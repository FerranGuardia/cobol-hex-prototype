"""Pipeline observatory — editorial local dashboard for the cobol-hex pipeline.

Single-file backend (`server.py`), single-page frontend (`templates.py`).
No external deps. Run via `app ui`. Polls `artifacts/` on each request.
"""
